# Salin file ini jadi .env untuk pengaturan lokal (opsional, default sudah localhost:5000).
# Di Render, isi VITE_API_URL di tab "Environment" pada Static Site frontend
# dengan URL backend yang sudah live, lalu redeploy agar ke-build ulang.

VITE_API_URL=http://localhost:5000
